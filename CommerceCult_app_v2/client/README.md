
# Client (Next.js)

This is a minimal Next.js frontend that uses the server API for Stripe sessions.

- Start dev: `npm run dev`
- Build: `npm run build`
- Start: `npm run start`

It expects NEXT_PUBLIC_API_URL and NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY in the environment.
